/__pycache__
/dist

# Theia build tree. The GUI is a `@theia/cli` browser-only app now (it moved
# here from apps/theia-preview, which carried these same rules); `theia build`
# generates all of this and none of it belongs in the repo.
node_modules/
lib/
src-gen/
gen-webpack*.js
gen-esbuild*.mjs
esbuild.mjs
.browser_modules/
